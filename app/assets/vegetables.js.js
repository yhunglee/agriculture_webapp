// Generated by CoffeeScript 1.10.0
(function() {
  var chart, display_or_not_veggie_list, myJSONObj;

  display_or_not_veggie_list = function() {
    var current_value;
    console.log("enter function");
    current_value = document.getElementByClassName("veggie-list").style.display;
    if (current_value === "none") {
      return document.getElementByClassName("veggie-list").style.display = "block";
    } else {
      return document.getElementByClassName("veggie-list").style.display = "none";
    }
  };

  myJSONObj = JSON.parse(gon.myDataV_json);

  chart = c3.generate({
    bindto: '#chart',
    data: {
      json: myJSONObj,
      keys: {
        x: 'date',
        value: ['total_average_price', 'total_transaction_quantity']
      },
      labels: false,
      axes: {
        total_transaction_quantity: 'y2'
      }
    },
    axis: {
      x: {
        type: 'timeseries',
        tick: {
          format: '%Y-%m-%d'
        }
      },
      y: {
        label: {
          text: 'average price',
          position: 'outer-middle'
        }
      },
      y2: {
        show: true,
        label: {
          text: 'transaction quantity',
          position: 'outer-middle'
        }
      }
    }
  });

}).call(this);
